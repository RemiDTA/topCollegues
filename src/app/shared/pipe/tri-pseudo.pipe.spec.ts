import { TriPseudoPipe } from './tri-pseudo.pipe';

describe('TriPseudoPipe', () => {
  it('create an instance', () => {
    const pipe = new TriPseudoPipe();
    expect(pipe).toBeTruthy();
  });
});
